//! `varve` — the PulseEngine toolchain layer manager.
//!
//! Status: design. See the README and `docs/manifest-format.md`.

#![forbid(unsafe_code)]

fn main() {
    eprintln!(
        "varve is not implemented yet — see \
         https://github.com/pulseengine/pulseengine.eu/issues/157"
    );
    std::process::exit(1);
}
