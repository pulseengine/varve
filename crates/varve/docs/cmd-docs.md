# varve docs [topic]

This documentation, embedded in the binary (offline). 'varve docs' lists topics; 'varve docs <topic>' shows one; --grep searches; 'varve docs check --coverage' asserts every subcommand has a topic (--strict fails on gaps).
